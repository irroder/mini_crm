<!-- Привет)

Для установки перенеси файлы в ide или редактор кода, перейди в директорию intra через cd ...

Пропиши sudo npm install --force с мака и npm install --force с винды.

Сделал кастомное решение, не понравится - перепишу как нужно.

Реализовал пагинацию в таблице, асинхронный запрос к серверу в контексте с кешированием через react query и необходимую
логику с созданием, редактированием и тд. -->
# mini_crm
